# Toran API URL (Required for both platforms)
# Used for fetching gateway configurations and sending logs
TORAN_API_URL=https://your-toran-api.vercel.app

# Redis URL (Optional - for response caching only)
# If not provided, response caching will be disabled
#
# Formats supported (credentials must be embedded in URL):
#   - Upstash REST: https://default:your-token@your-redis-instance.upstash.io
#   - Standard Redis: redis://default:password@host:port
#   - With query param: https://your-redis-instance.upstash.io?token=your-token
REDIS_URL=https://default:your-token@your-redis-instance.upstash.io

# Environment
ENVIRONMENT=production

# ============================================
# Platform-Specific Configuration
# ============================================

# For Vercel: Set these in .env or Vercel Dashboard
# For Cloudflare: Set these using: wrangler secret put <NAME>

# Vercel auto-detects from process.env.VERCEL
# Cloudflare auto-detects from ExecutionContext
